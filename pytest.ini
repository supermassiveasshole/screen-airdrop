[pytest]
testpaths = tests
norecursedirs = debug recovered .venv .git .pytest_cache .ruff_cache dist build
markers =
    real_data: tests using real fixture datasets
    regression_data: tests using captured debug regression datasets
