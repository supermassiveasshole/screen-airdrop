# Screen-Airdrop 项目进展汇报

## 1. 项目背景

当前开发环境运行在 **Citrix / 远程桌面隔离环境**中，存在几个明显限制：

* clipboard 重定向被禁
* 文件传输通道不可用
* 网络访问受限
* 本地工具无法直接访问远端环境

这带来的实际问题是：

```
本地与远端之间几乎没有数据交换通道
```

包括：

* 日志复制
* 代码片段
* 配置
* 小脚本

因此我们尝试实现一种 **不依赖系统权限的数据通道**：

**Screen-Airdrop**

基本思路是：

```
数据
↓
视觉编码
↓
远端屏幕显示
↓
本地截图识别
↓
恢复数据
```

也就是利用 **屏幕作为单向数据链路**。

优点：

* 不依赖 clipboard
* 不依赖文件系统
* 不依赖网络
* 只需要显示和截图能力

---

# 2. 当前实现进展

目前已经完成 **端到端原型**，并进行了多轮性能优化。

当前系统流程：

```
sender
↓
frame encoding
↓
屏幕显示
↓
receiver screenshot
↓
frame decode
↓
data reconstruction
```

当前系统运行在：

```
≈ 12 FPS
```

的发送帧率下。

---

# 3. 当前性能

在当前帧率条件下，系统可以达到：

```
≈ 40 KB/s
```

的稳定吞吐。

这个速度已经可以支持：

* 小脚本
* 配置文件
* 日志片段
* 中等长度文本

因此 **视觉链路本身已经验证是可行的**。

---

# 4. 当前支持的功能方向

目前系统主要在探索三个实际用途。

---

## 1）远端文本 / 日志导出

最直接的应用是：

```
remote → screen → local
```

把远端终端里的：

* 日志
* 命令输出
* 文本

通过视觉编码传输到本地恢复。

这一部分已经基本可用。

---

## 2）剪切板桥接

第二个目标是实现一个 **单向 clipboard bridge**：

```
remote clipboard
↓
screen encode
↓
local decode
↓
local clipboard
```

这样可以实现：

```
remote → local
复制文本
```

绕过 Citrix 禁用 clipboard 的限制。

目前正在规划这一部分的协议和工具集成。

---

## 3）文本 / 文件输入

除了数据导出之外，还在探索 **反向输入通道**。

目标是可以把：

* shell command
* Tcl script
* 小文件

输入到远端环境。

思路是：

```
local data
↓
视觉编码
↓
远端解码
↓
模拟键盘输入
```

在编码上目前考虑使用：

```
base26
```

即只使用：

```
a-z
```

字符进行编码。

这样可以避免：

* Shift
* 符号
* 键盘布局差异

提高 **macOS + Citrix 输入稳定性**。

通过 base26 可以把：

```
binary
↓
a-z
```

再在远端恢复成文本或文件。

---

# 5. 当前主要技术挑战

目前系统的主要挑战集中在三个方面。

---

## 1）截图帧率限制

当前接收端截图频率大约：

```
≈ 12 FPS
```

这是当前链路带宽的主要瓶颈。

来源包括：

* remote desktop capture
* macOS screenshot API

---

## 2）丢帧问题

视觉链路不可避免存在：

* 丢帧
* 解码失败
* 帧顺序变化

当前主要依赖：

```
循环发送
```

来保证最终恢复。

但效率仍有提升空间。

---

## 3）信息密度

当前每帧的数据密度仍然有优化空间，例如：

* block 布局
* header 开销
* 编码方式

---

# 6. 正在探索的协议方向

接下来主要在设计 **更强的传输协议**，包括：

### 1）分组传输

```
file
→ groups
→ frames
```

减少循环周期。

---

### 2）纠错编码

考虑加入：

```
erasure coding
```

允许接收端在 **部分丢帧情况下恢复数据**。

---

### 3）喷泉码

进一步探索：

```
fountain code
```

这种方式在高丢帧链路上更鲁棒。

---

### 4）更高信息密度

例如：

* 更紧凑的 frame layout
* 多 block 并行
* 灰度编码

提高 **每帧信息量**。

---

# 7. 下一步计划

短期目标主要有三个：

### 1）稳定协议

确定：

* frame header
* group structure
* 编码格式

---

### 2）提升带宽

重点优化：

* frame layout
* block 并行
* 编码效率

---

### 3）实现应用层工具

逐步实现：

* clipboard bridge
* 文本输入
* 小文件传输

---

# 8. 总结

目前 Screen-Airdrop 已经：

* 完成端到端原型
* 在 **12 FPS** 条件下达到 **≈40 KB/s**
* 验证了视觉链路的可行性

下一阶段重点在：

```
纠错
协议优化
应用层功能
```

逐步把它发展成一个 **在受限环境下可用的数据交换通道**。
